# Extra dependencies for tools/analyze_gesture_log.py (offline log analysis
# only — not needed to actually fly the drone, so kept out of
# requirements.txt). Install with:
#   pip install -r requirements-analysis.txt
scikit-learn>=1.3.0
matplotlib>=3.7.0
pandas>=2.0.0
